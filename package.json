{
  "name": "toasttimer",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.0.0"
  },
  "devDependencies": {
    "react-scripts": "1.0.17"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy-dev": "aws s3 sync --delete build/ s3://dev.toasttimer.com/",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
